import { DRAWER_WIDTH } from '@/constants/Misc'
import { styled, Stack } from '@mui/material'

export const FormSidebarContainer = styled(Stack)({
  width: DRAWER_WIDTH,
  paddingLeft: 20,
  paddingRight: 20,
})
